# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Note: the local PC uses port 55432 instead of the standard 5432 to
# avoid local Postgres port conflicts.
# Note: for dev, the host is localhost.
DATABASE_URL=postgresql://postgres:postgres@localhost:55432/premier_league